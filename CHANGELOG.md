# 更新记录

## v1.4.3

- 修复差异地图遮挡正文和折叠把手的问题，纯文本与 OCR 模式同步生效
- 最近比较中的长文件名从中间省略并保留扩展名，格式角标不再被挤压
- Windows 10 使用系统标题栏，避免窗口右上角出现灰块；Windows 11 和 Linux 保留按钮覆盖层
- 合并相邻且无差异的文字节点，减少大文档的 DOM 数量；优化侧栏折叠与差异地图测量性能
- 启动日志增加系统版本、标题栏模式和 GPU 加速状态

## v1.4.2

- 修复 Windows 安装时可能出现的「DocDiff cannot be closed / Error opening file for writing: Uninstall DocDiff.exe」：安装程序会先请求退出、再强制结束残留进程，并处理被占用的旧卸载程序
- 修复退出时 OCR 进程可能残留导致应用不结束的问题
- 右侧折叠把手不再被差异地图挡住，收起后完整显示
- 窄窗口下审阅条不再重叠（界面按窗口宽度自适应，最小宽度降到 960）
- 修订审阅里按 U 撤销最近一次决定并跳回那一处

## v1.4.1

- 设置图标换成齿轮；快捷键表重排为分组对齐的两列
- 左右侧边栏改成边缘中部的圆形折叠把手，左右对称
- 折叠后图标上的差异数改成右上角小圆角标
- 内置思源黑体（Noto Sans SC），macOS 和 Windows 的中文显示完全一致
- 「最近比较」保留 12 条，单条删除按钮常显，新增「清空」

## v1.4

- 比较界面左右侧边栏都可以折叠，给对比区更多空间
- 新增设置面板（语言、外观、音效、快捷键）
- 新增中英文切换：English 下界面、菜单、导出文件名都是英文

## v1.3

- 应用正式命名为 DocDiff
- 提供 macOS 安装镜像（.dmg）和 Windows 安装程序（.exe）
- Mac 图标改为满版设计，macOS 26 不再给图标套灰色边框
- 新增 5 张商店宣传图

## v1.2

- 新 Logo 和应用图标；深色模式下文档纸张改为深色
- 富文本里鼠标悬停框出对应行，点击把两侧对齐到这一行（Esc 撤销）
- 能识别"一段被拆成几段 / 几段合成一段"，PDF 与 Word 互比时不再误报

## v1.1

- 界面改成 Duolingo 风格
- 新增最近比较、粘贴文字、差异地图、修订审阅流程（进度条 + 完成页）
- 文件变化自动提示、复制变更摘要、忽略全角/半角标点、OCR 取消和完成通知
- 设置和窗口位置会被记住
